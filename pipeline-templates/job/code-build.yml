parameters:
  SolutionBaseName:
  BuildConfiguration:

jobs:
- job: CodeBuild
  pool:
    name: DAS - Continuous Integration Agents
    demands: LATEST_DOTNET_VERSION -equals 3.1
  workspace:
    clean: all
  variables:
  - group: BUILD Management Resources  
  steps:
  - checkout: self
    fetchDepth: 0

  - template: azure-pipelines-templates/build/step/gitversion.yml@das-platform-building-blocks

  - template: azure-pipelines-templates/build/step/app-build.yml@das-platform-building-blocks
    parameters:
      SonarCloudProjectKey: SkillsFundingAgency_das-roatp-assessor
      ContinueOnVulnerablePackageScanError: true

  - task: DotNetCoreCLI@2
    displayName: "Publish - dotnet publish SFA.DAS.RoatpAssessor.Web"
    inputs:
      command: publish
      publishWebProjects: false #set to false as this setting (which defaults to true) will parse the entire repo for web projects
      projects: "src/SFA.DAS.RoatpAssessor/SFA.DAS.RoatpAssessor.Web/SFA.DAS.RoatpAssessor.Web.csproj"
      arguments: "-o $(build.artifactstagingdirectory)/publish -c ${{ parameters.BuildConfiguration }} --no-build"
      modifyOutputPath: true
      zipAfterPublish: true

  - task: CopyFiles@2
    displayName: "Copy Files to: $(build.artifactstagingdirectory)"
    inputs:
      Contents: |
        azure/**
      TargetFolder: "$(build.artifactstagingdirectory)/publish"
      OverWrite: true

  - task: PublishPipelineArtifact@1
    displayName: "Publish Artifact"
    inputs:
      targetPath: "$(build.artifactstagingdirectory)/publish"
      artifactName: ${{ parameters.SolutionBaseName }}